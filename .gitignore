node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
nul

.env

# Claude Code local (per-user) settings
.claude/settings.local.json

# Merge/patch artifacts (never commit these)
*.orig
*.rej

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Admin content editor write-target artifact (server/routes/admin.ts) — never read, do not track
data/actions.json.backup
