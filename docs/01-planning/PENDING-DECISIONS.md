# Pending Decisions — Nes's Queue

*One entry per open product/strategy decision. Updated in place (resolved entries are deleted, not struck). Last updated: July 5, 2026, evening session (Tier 2 forks A–F DECIDED by Nes — §1 now gates only on spec approval, [DRAFT] spec written; former §5 mood phases 6–10 resolved via fork E and removed; earlier same day: merge day + meeting-relevance Tier 0+1 arc, former §7 C74 resolved and removed).*

---

## 1. Tier 2 (event-driven meetings) — SPEC APPROVED, executing
**Resolved upstream (July 5, 2026):** Nes chose **Tier 0+1 as one planned slice** — plan: `implementation-specs/COMPLETED/[COMPLETE] meeting-relevance-tier0-1-plan.md` (EXECUTED — PRs #122–#124 merged July 5, 2026; verifier-confirmed — the one verifier finding, C74/header AUTO review-gate bypass, was resolved July 5, 2026, see backlog C74). **What remains pending:** Tier 2 (meetings fire *because* something happened) is deferred by decision and must be **designed jointly with the deferred side-events system** — they'd share an event model (`events.json` is already key-clean per revival PR-8); neither should be planned in isolation. The Tier 0+1 selection pipeline is built as staged pure functions specifically so Tier 2 slots in as a pre-draw stage (plan §5). **Unblocks when decided:** the "timeliness" half of the relevance problem + the phantom side-events system. **Defer cost:** low while Tier 0+1 is unplaytested — timeliness feedback should come from playing the weighted version first.

**FORKS DECIDED and SPEC APPROVED (July 5, 2026, evening session, Nes): A1, B1, C2, D1, E and F per recommendation; spec approved with three amendments** (urgency indicator on exec cards → PR-2; mood_crater derivation-completeness verification → PR-1; named playtest gate after PR-2 — orchestrator pings Nes, he decides play-now vs. continue). Spec promoted to `implementation-specs/[READY] tier2-reactive-meetings-and-side-events-plan.md`; the 5-PR factory is executing. **The one decision still owed by Nes inside this arc is the playtest-gate answer after PR-2 merges.** This entry closes when the arc lands (spec → COMPLETED). The design-space record below is kept for rationale.

**The design space as presented:** The forks: **A** event model (rec: A1 — stateless `deriveWeekHappenings` derivation from persisted tables — charts, releases, `mood_events`, `artists.signedWeek`, project stages — sibling of `deriveRelevanceState`; vs A2 persisted queue); **B** cadence entry (rec: B1 — reactive meeting *replaces* that exec's weekly draw as a pre-draw injection stage per the Tier 0+1 plan's §5 slot-in guarantee, with "why now" line; guardrails: 1/exec/week, trigger priority, cooldowns, 1–2-week expiry); **C** side-events surface (rec: C2 — week-transition choice beat in the staged WeekSummary reveal, choice-time effect application per the dialogue precedent `server/routes/artists.ts:99-131`; vs C1 email-with-choices); **D** RNG discipline (rec: D1 — keep the existing in-stream `ctx.getRandom` draw at `game-engine.ts:967` so the golden master needs zero re-bless — its fixtures pin `weekly_chance: 0` but the draw IS in their RNG stream — and move event *selection* to an isolated seed honoring the dead `event_weights`/`event_cooldown`/`max_events_per_week` config, closing C64); **E** mood phases 6–10 disposition — Nes folded former §5 fully into this call (rec: Phase 7 subsumed as mood happenings; Phase 6 spun off as an independent small dialogue slice; Phase 9 anticipated as future weight tuning only; Phases 8/10 stay deferred); **F** scope (rec: one two-slice arc — MVP-1 reactive meetings with ~5–6 authored reactive meetings route-side/zero-GM-impact, then MVP-2 side events surfaced; success/failure rolls anticipated in schema but unimplemented; mood-availability gates ignored — sit-out already covers exec presence rhythm). Key research facts: side events are half-alive (weekly 20% roll already burns a golden-master-stream RNG draw; 13 authored events with all-live effect keys; result invisible — no UI reads `summary.events`), and every canonical trigger is already persisted with a week number, so Tier 2 meetings can stay route-side like Tier 0+1.

## 2. C42 — awareness → wire further or stop celebrating it
**Q:** Awareness is live in streaming revenue but weakly surfaced — invest in full player-facing integration (release page/dashboard readouts per `[FUTURE] awareness-system-design.md`) or trim the celebration to match its weight? **Unblocks:** Release-Experience Tier 2 AND the awareness design doc (three docs queue on this one call). **Defer cost:** a live economic system stays invisible; WeekSummary keeps celebrating a stat players can't reason about. **Session evidence:** C69 (fixed July 4 on PR #119) showed marketing-driven awareness gain was silently dead for ~9 months and nobody noticed — strong signal the system's invisibility is itself the bug. The Slice A "Buzz" tooltip (PR #120) now explains the meeting channel, which slightly raises the cost of removing awareness later.

## 3. C43 — cancel/reschedule planned releases
**Q:** Should planned releases be cancellable/reschedulable, and what's the refund rule? Server DELETE endpoint with safe refund already exists; only UI + refund-policy decision missing. **Unblocks:** Release-Experience Tier 3. **Defer cost:** money committed to a distant release week stays unrecoverable; inconsistent with tours (60% refund).

## 4. C32 — email snapshot ~10k cap
**Q:** Raise/chunk the email cap or accept it? **Deferred with quantified justification** (cap is ~20–100× above real campaign volume; truncation surfacing already shipped). **Unblocks:** nothing material. **Defer cost:** effectively zero. Recommend leaving deferred indefinitely.

*(Former entry 5 — Artist Mood Phases 6–10 — resolved and removed July 5, 2026, evening session: Nes folded it into the §1 Tier 2 design call and decided fork E per recommendation — Phase 7 subsumed into the shared event model as mood happenings; Phase 6 spun off as an independent small dialogue slice; Phase 9 anticipated as future tuning only; Phases 8 + 10 stay deferred pending player feedback. Recorded in the [DRAFT] Tier 2 spec §4.)*

## 6. Desktop GUI migration — option A/B/C
**Q:** Cloud-dependent (A, ~60% effort) vs local-first hybrid (B, ~120%) vs full offline (C, ~250%) — gated on 5 framing questions (offline? multiplayer? mobile? timeline? monetization?). **Unblocks:** all of `desktop-gui-migration-strategy.md`. **Defer cost:** none while web-first development continues.

*(Former entry 7 — C74, GameHeader AUTO review-gate bypass — resolved and removed July 5, 2026: Nes chose to route the header AUTO button through the same Option A review panel (the old direct-commit path was removed); the same slice also fixed AUTO proposing the A&R exec while the A&R office slot was in use. See backlog C74.)*

*(Earlier former entry 7 — PR #119/#120 merge timing — resolved and removed July 5, 2026: both merged, plus the docs pass, as `bbcacef`/`e6b4723`/`d3ddc2f`.)*

---

## Stopped mid-session (gated territory hit)
- **C62 sub-item 1 — `artistsSuccessful`/`projectsCompleted` campaign score components** (stopped inside the C62 slice, commit `f1b1315`): no doc defines "successful artist"/"completed project" semantics, and `AchievementsEngine.calculateCampaignResults` receives only the `gameState` row (no artist/project data), so implementing it needs a **design decision on the semantics** plus call-site plumbing through `ProgressionProcessor.checkCampaignCompletion`. Expanded TODO left at the site; sub-items 2–3 (Media Mogul tiers, 52-week copy) shipped.
